using System.Collections.ObjectModel;
using Microsoft.DotNet.Scaffolding.Shared.Messaging;

public class Suply 
{
    public string Title { get; set; }

    //database have some count
    //this count should to fill
    //also database count will change from analytic
    //this service will automatic create excel suply act and used postal service to send vendor

    // get order to production -> order material -> logistic to storage -> receiving material -> trasfer material to work position -> load plc program -> logistic complite goods to storage -> show on marketplace -> logistic to customer and used crm service

    public int TakeNews()
    {
       
    }
}