#delete reference local/remote > git > settings remote > clear

#create reference local/remote > git > create

#commit reference local/remote > git > stage and or hide > stage > again git > jurnal > select outcoming brench > sent